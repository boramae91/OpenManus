# 🚀 개선된 주식 분석 시스템 가이드

## 📋 개선된 플로우 개요

기존 플로우에서 **실제 재무데이터를 활용**하도록 대폭 개선했습니다!

### 🔄 **기존 플로우 vs 개선된 플로우**

| 단계 | 기존 플로우 | 🆕 개선된 플로우 |
|------|-------------|------------------|
| 1 | 프롬프트 입력 | 프롬프트 입력 |
| 2 | 종목 감지 | **종목+종목코드 정밀 감지** |
| 3 | Classifier 실행 | **📊 실제 재무데이터 수집 (DART + yfinance)** |
| 4 | 분석 에이전트 | **🏷️ 재무데이터 기반 정밀 분류** |
| 5 | JSON 저장 | **📈 재무데이터 기반 상세 분석** |
| 6 | - | **💾 종합 결과 JSON 저장** |

## 🛠️ 설치 및 설정

### 1. **필수 패키지 설치**
```bash
pip install -r requirements_enhanced.txt
```

### 2. **환경변수 설정 (.env 파일)**
```env
# 필수: OpenAI API 키 (실제 키를 입력하세요)
OPENAI_API_KEY=your_openai_api_key_here

# 선택사항: DART API 키 (한국 기업 더 상세한 정보)
DART_API_KEY=your_dart_api_key_here

# LLM 설정
LLM_MODEL=gpt-4o
```

## 🚀 실행 방법

### **개선된 시스템 실행**
```bash
python enhanced_main.py
```

### **기존 시스템과 비교 실행**
```bash
# 기존 시스템
python main.py

# 개선된 시스템
python enhanced_main.py
```

## 📊 주요 개선사항

### 1. **실제 재무데이터 기반 분석**
- **yfinance**: 실시간 주가, PER, PBR, ROE 등 핵심 지표
- **DART API**: 한국 기업 공시정보, 상세 재무제표 (선택사항)
- **데이터 품질 평가**: 수집된 데이터의 신뢰도 자동 평가

### 2. **정량적 지표 기반 정밀 분류**
```python
# 예시: 자동 계산되는 정량적 기준
- 고성장주: 매출성장률 15% 이상 + 혁신업종
- 우량주: 시가총액 10조원 이상 + ROE 10% 이상
- 자산주: PBR 1.0 미만 + 자산가치 높음
```

### 3. **종합적인 결과 저장**
```json
{
  "user_prompt": "사용자 질문",
  "steps": {
    "step1_stock_detection": "종목 감지 결과",
    "step2_financial_data": "실제 재무데이터",
    "step3_classification": "정밀 분류 결과",
    "step4_detailed_analysis": "상세 분석 결과"
  },
  "final_summary": "종합 요약",
  "data_quality": "데이터 품질 평가"
}
```

## 🎯 테스트 예시

### **테스트 질문들**
```
1. "삼성전자 주식 분류해줘"
2. "005930 어떤 종류의 주식이야?"
3. "Apple(AAPL) 투자 분석해줘"
4. "카카오 재무상태 어때?"
```

### **예상 개선 효과**
- ✅ **정확도 향상**: 실제 재무지표 기반 객관적 분석
- ✅ **신뢰도 향상**: 데이터 출처 및 품질 명시
- ✅ **일관성**: 정량적 기준으로 동일한 결과 보장
- ✅ **투명성**: 분석 근거를 수치로 제시

## 📁 파일 구조

```
📁 개선된 시스템 구조
├── enhanced_main.py                    # 🆕 개선된 메인 실행 파일
├── app/
│   ├── data_collector/                 # 🆕 재무데이터 수집 모듈
│   │   ├── __init__.py
│   │   └── financial_data_collector.py
│   ├── agent/
│   │   ├── stock_classifier.py         # 기존 (재무데이터 연동)
│   │   └── manus.py                   # 기존 (재무데이터 연동)
│   └── ...
├── requirements_enhanced.txt           # 🆕 추가 패키지 포함
└── results/                           # 🆕 개선된 결과 저장소
    └── enhanced_analysis_*.json
```

## 🔍 트러블슈팅

### **자주 발생하는 문제들**

1. **yfinance 연결 실패**
   ```
   해결: 인터넷 연결 확인, 종목코드 정확성 확인
   ```

2. **DART API 키 없음**
   ```
   해결: yfinance만으로도 충분히 동작 (선택사항)
   ```

3. **패키지 설치 오류**
   ```bash
   pip install --upgrade pip
   pip install -r requirements_enhanced.txt
   ```

## 🎉 기대 효과

| 개선 영역 | 기존 시스템 | 개선된 시스템 |
|-----------|-------------|---------------|
| **데이터 소스** | AI 추론만 | 실제 재무데이터 + AI 분석 |
| **정확도** | 일반적 지식 기반 | 실시간 정량 지표 기반 |
| **신뢰도** | 주관적 평가 | 객관적 데이터 + 검증 |
| **일관성** | 질문마다 다름 | 동일 기준 적용 |
| **투명성** | 근거 불명확 | 수치 기반 명확한 근거 |

---

## 🎯 다음 단계

이 개선된 시스템을 통해 **더욱 정확하고 신뢰할 수 있는 주식 분석**을 경험해보세요!

1. **실행**: `python enhanced_main.py`
2. **테스트**: 다양한 종목으로 분석 시도
3. **결과 확인**: `results/` 폴더의 JSON 파일들
4. **비교**: 기존 시스템 vs 개선된 시스템

**정말 중요한 과제를 위한 최고의 도구가 준비되었습니다!** 🚀
