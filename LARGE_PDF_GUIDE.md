# 📊 대용량 PDF 보고서 분석 시스템 사용 가이드

OpenManus의 새로운 **대용량 PDF 분석 시스템**을 사용해서 60만 글자 이상의 사업보고서나 분기보고서를 AI로 완전히 분석하고 JSON 파일로 저장하는 방법을 안내합니다.

## 🎯 시스템 특징

### ✨ 주요 기능
- **무제한 크기**: 60만 글자 이상의 대용량 PDF도 안전하게 처리
- **GPT-4o 사용**: 최고 성능의 AI 모델로 전문적인 분석
- **스마트 분할**: 보고서 구조를 파악해서 의미 있는 섹션으로 분할
- **전문 분석**: 재무 전문가 수준의 상세한 분석 제공
- **JSON 저장**: 구조화된 결과를 체계적으로 저장

### 🔧 처리 과정
1. **PDF 텍스트 추출** - 전체 원문을 무제한 크기로 추출
2. **구조 분석** - AI가 보고서 구조를 자동으로 파악
3. **섹션별 분석** - 각 청크를 재무 전문가 관점에서 분석
4. **통합 리포트** - 모든 분석을 종합한 완전한 투자 리포트 생성
5. **핵심 인사이트** - 가장 중요한 투자 포인트만 간추린 요약
6. **JSON 저장** - 모든 결과를 체계적으로 정리해서 저장

## 🚀 빠른 시작

### 1. 환경 설정 확인

먼저 OpenAI API 키가 설정되어 있는지 확인하세요:

```bash
# .env 파일에 다음 내용이 있어야 합니다:
OPENAI_API_KEY=sk-your-openai-api-key-here
```

### 2. 시스템 테스트

실제 분석 전에 시스템이 정상 작동하는지 테스트하세요:

```bash
python test_large_pdf.py
```

### 3. 대용량 PDF 분석 실행

메인 분석 프로그램을 실행하세요:

```bash
python large_pdf_main.py
```

## 📖 상세 사용법

### 1단계: 프로그램 실행

```bash
python large_pdf_main.py
```

프로그램이 시작되면 다음과 같은 환영 메시지가 나타납니다:

```
🚀 대용량 PDF 보고서 분석 시스템
==================================================
이 시스템은 60만 글자까지의 대용량 보고서를 AI로 분석해서
체계적인 JSON 파일을 생성합니다. (GPT-4o 사용)
```

### 2단계: PDF 파일 경로 입력

분석할 PDF 파일의 경로를 입력하세요:

```
PDF 파일 경로: reports/삼성전자_2024_사업보고서.pdf
```

**💡 팁**:
- 파일을 드래그 앤 드롭하면 경로가 자동으로 입력됩니다
- 따옴표로 둘러싸인 경로도 자동으로 처리됩니다

### 3단계: 회사 정보 입력

```
회사명 (선택사항): 삼성전자
```

### 4단계: 보고서 종류 선택

```
📊 보고서 종류를 선택해 주세요:
1. 사업보고서
2. 분기보고서
3. 반기보고서
4. 기타

선택 (1-4): 1
```

### 5단계: 분석 실행

설정 확인 후 분석을 시작합니다:

```
📋 분석 설정 확인:
   파일: reports/삼성전자_2024_사업보고서.pdf
   회사: 삼성전자
   종류: 사업보고서
==================================================

분석을 시작하시겠습니까? (y/n): y
```

### 6단계: 분석 진행 확인

시스템이 단계별로 진행 상황을 보여줍니다:

```
🔧 AI 분석 시스템 초기화 중...
✅ 시스템 초기화 완료!
🤖 사용 모델: GPT-4o
📊 처리 용량: 무제한 (60만+ 글자 지원)

🔍 대용량 PDF 분석 시작...
⏱️ 대용량 파일의 경우 몇 분이 소요될 수 있습니다...

📖 1단계: 대용량 PDF 텍스트 추출 중...
✅ 텍스트 추출 완료: 623,450자 (약 62.3만 글자)

🔍 2단계: 보고서 구조 분석 및 스마트 분할 중...
📊 총 25개 청크 생성 완료

🔬 3단계: 섹션별 AI 전문 분석 수행 중...
📊 섹션 1/25 분석 중... (길이: 24,850자)
✅ 섹션 1 분석 완료
...

🎯 4단계: 통합 분석 및 핵심 인사이트 추출 중...
💎 5단계: 핵심 투자 인사이트 추출 중...
```

### 7단계: 결과 확인

분석 완료 후 결과를 확인합니다:

```
==================================================
🎉 분석 완료!
==================================================
✅ 분석 성공!
📄 처리된 텍스트: 623,450자
🧩 분석 섹션 수: 25개
⏱️ 총 처리 시간: 248.3초
💾 저장 경로: results/large_pdf_analysis-삼성전자-사업보고서-20241217_143052.json

💎 핵심 인사이트 미리보기:
🎯 투자 핵심 포인트:
1. AI 반도체 수요 급증으로 HBM 매출 300% 증가, 메모리 사업 수익성 대폭 개선
2. 파운드리 사업 고부가가치 제품 비중 확대로 수익성 향상 기대
3. 모바일 부문 플래그십 제품 강화와 폴더블 시장 점유율 확대...

📁 상세 분석 결과는 JSON 파일에서 확인하세요:
   results/large_pdf_analysis-삼성전자-사업보고서-20241217_143052.json
```

## 📁 결과 파일 구조

생성되는 JSON 파일의 구조는 다음과 같습니다:

```json
{
  "metadata": {
    "pdf_path": "분석한 PDF 파일 경로",
    "report_type": "보고서 종류",
    "company_name": "회사명",
    "success": true,
    "total_text_length": 623450,
    "total_chunks": 25,
    "total_processing_time": "248.3초"
  },
  "extraction_info": {
    "success": true,
    "text_length": 623450,
    "extraction_method": "pdfplumber",
    "total_pages": 245
  },
  "structure_analysis": {
    "total_chunks": 25,
    "structure_analysis": "AI가 분석한 보고서 구조 설명"
  },
  "section_analyses": {
    "section_1": {
      "content_length": 24850,
      "analysis": "섹션 1의 상세한 AI 분석 결과",
      "analysis_timestamp": "2024-12-17T14:30:52"
    }
    // ... 각 섹션별 분석 결과
  },
  "integrated_summary": {
    "success": true,
    "integrated_report": "전체 섹션을 통합한 완전한 투자 리포트"
  },
  "key_insights": {
    "success": true,
    "key_insights": "가장 중요한 투자 인사이트 요약"
  }
}
```

## 🔧 고급 설정

### 청크 크기 조정

더 세밀한 분석이 필요한 경우 청크 크기를 조정할 수 있습니다:

```python
# app/utils/large_pdf_analyzer.py 수정
analyzer = LargePDFAnalyzer()
analyzer.chunk_processor.chunk_size = 30000  # 30KB로 증가
analyzer.chunk_processor.overlap_size = 3000  # 겹침도 증가
```

### 사용자 정의 분석 함수

특별한 분석이 필요한 경우 사용자 정의 분석 함수를 만들 수 있습니다:

```python
async def custom_analysis_function(chunk):
    # 특별한 분석 로직
    return {
        "custom_analysis": "사용자 정의 분석 결과",
        "special_metrics": ["지표1", "지표2"]
    }

# 분석 시 사용자 정의 함수 적용
result = await analyzer.analyze_large_report(
    pdf_path=pdf_path,
    analysis_function=custom_analysis_function
)
```

## 🚨 문제 해결

### 일반적인 문제들

1. **OpenAI API 키 오류**
   ```
   ❌ 오류: OPENAI_API_KEY 환경변수가 설정되지 않았습니다.
   ```
   **해결방법**: `.env` 파일에 `OPENAI_API_KEY=your-key-here` 추가

2. **PDF 파일을 찾을 수 없음**
   ```
   ❌ 파일을 찾을 수 없습니다: /path/to/file.pdf
   ```
   **해결방법**: 파일 경로를 다시 확인하고 파일이 존재하는지 확인

3. **메모리 부족 오류**
   ```
   ❌ PDF 텍스트 추출 오류: Memory error
   ```
   **해결방법**: 청크 크기를 줄이거나 시스템 메모리를 늘리세요

4. **분석 시간 초과**
   ```
   ❌ AI 분석 테스트 실패: Request timeout
   ```
   **해결방법**: 네트워크 연결을 확인하고 API 요청 제한을 확인하세요

### 시스템 테스트

문제가 발생하면 먼저 시스템 테스트를 실행하세요:

```bash
python test_large_pdf.py
```

테스트 결과에 따라 문제점을 파악할 수 있습니다.

## 💡 성능 최적화 팁

1. **적절한 청크 크기**: 25KB가 기본값이지만, 내용에 따라 조정 가능
2. **병렬 처리 제한**: 메모리 사용량을 고려해서 순차 처리 사용
3. **중간 저장**: 대용량 파일의 경우 중간 결과를 저장해서 복구 가능
4. **API 요청 제한**: OpenAI API 제한을 고려해서 요청 간격 조정

## 📞 지원 및 문의

- 시스템 관련 문의: GitHub Issues 활용
- 기술적 문제: 로그 파일과 함께 문의
- 기능 개선 요청: Pull Request 환영

---

이 시스템을 사용해서 대용량 보고서를 효율적으로 분석하고 투자 인사이트를 얻으시기 바랍니다! 🚀
